#include <iostream> // cout
using namespace std; // некая стандартная именованная область кода, в которой размещены основные команды cin cout string system



int main()
{
    setlocale(0, "rus");
    /* float S, V;     // task 1
     int t;
     int hours;
     int minutes;
     int second;
     cout << "Введите расстояние в км: ";
     cin >> S;
     cout << "Введите скорость в м/c: ";
     cin >> V;
     
     S *= 1000;
     t = S / V;
     
     hours = t / 3600;
     minutes = (t - hours * 3600) / 60;
     second = t - (hours * 3600 + minutes * 60);
     
     cout << "Часы: " << hours << endl;
     cout << "Минуты: " << minutes << endl;
     cout << "Секунды: " << second << endl;
     */
    
    
    
    /* double mass; // task 2
     cout << "Ввести массу в тоннах: ";
     cin >> mass;
     int tons;
     int kg; // целая часть
     float gramm; // дробная часть
     
     tons = mass;
     kg = (mass - tons) * 1000;
     gramm = (mass - tons - (float (kg) / 1000)) * 1000000;
     cout << tons << "т." << kg << "кг." << gramm << "грамм.";
     */
    
    
    /* int time;  // task 3
     cout << "Ввести время в секундах: ";
     cin >> time;
     int hours;
     int minutes;
     int second;
     
     hours = time / 3600;
     minutes = (time - hours * 3600) / 60;
     second = time - (hours * 3600 + minutes * 60);
     cout << hours << "Часы:" << minutes << "Минуты:" << second << "Секунды:";
     */
    
}
