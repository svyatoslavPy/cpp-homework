#include <iostream>
using namespace std;



int main()
{
    setlocale(0, "rus");
      // TASK 1
    /*
    double number1, number2, result1, result2, result3, result4;
    string operation;
    cout << "Введите число: ";
    cin >> number1;
    cout << "Введите второе число: ";
    cin >> number2;
    cout << "Введи одну из операций + , - , * , / : ";
    cin >> operation;
    if (operation == "+") {
        result1 = number1 + number2; // сумма
        cout << "Результат суммы: " << result1;
    }
    else if (operation == "-") {
        result2 = number1 - number2; // вычитание
        cout << "Результат вычитания: " << result2;
    }
    else if (operation == "*") {
        result3 = number1 * number2; // умножение
        cout << "Результат умножения: " << result3;
    }
    else if (operation == "/") {
        result4 = number1 / number2; // деление
        cout << "Результат деления: " << result4;
        */
    }
    

    
    /* // TASK 2
    double number1, number2, number3, min;
    cout << "Введите первое число: ";
    cin >> number1;
    cout << "Введите второе число: ";
    cin >> number2;
    cout << "Введите третье число: ";
    cin >> number3;
    if ((number1 < number2) && (number2 < number3))  {
        min = number1, number2, number3;
        cout << "Минимальное число: " << min;
    }
    */



    /* // TASK 3
    double number;
    cout << "Введите число: "; // выяснить положительное ли оно, отрицательное, или равно нулю
    cin >> number;
    if (number > 0) {
        cout << "Число положительное: " << number;
    }
    else if (number < 0) {
        cout << "Число отрицательное: " << number;
    }
    else if (number == 0) {
        cout << "Число равно нулю: " << number;
    }
    */

}
